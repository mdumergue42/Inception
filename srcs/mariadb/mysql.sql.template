CREATE DATABASE IF NOT EXISTS wordpress;
CREATE USER IF NOT EXISTS '${SQL_USER}'@'%' IDENTIFIED BY '${SQL_PASS}';
GRANT ALL PRIVILEGES ON wordpress.* TO '${SQL_USER}'@'%' WITH GRANT OPTION;
FLUSH PRIVILEGES;